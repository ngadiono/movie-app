// Vendors
import { styled } from '@mui/material/styles';

export const Wrapper = styled('div')({
  minHeight: '50px',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  marginTop: 20,
});
