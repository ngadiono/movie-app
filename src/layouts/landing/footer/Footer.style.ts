// Vendors
import { styled } from '@mui/material/styles';

export const Wrapper = styled('div')({
  minHeight: '50px',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  position: 'absolute',
  left: '0',
  right: '0',
  bottom: '0',
});
